Rebuild scaffold for NetTech

This folder contains minimal scaffolding to start a fresh rebuild while keeping API compatibility.

- backend/: minimal Node/Express server template
- frontend/: minimal React + Vite app template
- docker-compose.yml: local dev composition with MongoDB

To customize, replace templates with full implementations and wire to the `openapi.yaml` file at repository root.
