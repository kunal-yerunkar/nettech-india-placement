{
  "name": "nettech-india-placement-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "backend"
  ],
  "scripts": {
    "dev:client": "npm --workspace client run dev",
    "dev:server": "npm --workspace backend run dev",
    "build:client": "npm --workspace client run build",
    "seed": "npm --workspace backend run seed"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "mongodb": "^7.0.0"
  }
}
